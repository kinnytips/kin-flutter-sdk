import 'package:kin_base/base/network/services/app_info_providers.dart';

class AppUserAuthInterceptor {
  AppInfoProvider appInfoProvider() {
    ClientInterceptor() {
       // Metadata.Key<String> HEADER_KEY_APP_USER_ID = Metadata.Key.of("app-user-id", Metadata.ASCII_STRING_MARSHALLER);
       // Metadata.Key<String> HEADER_KEY_APP_USER_PASSKEY = Metadata.Key.of("app-user-passkey", Metadata.ASCII_STRING_MARSHALLER);
    }
  }
}
